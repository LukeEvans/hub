version: "3.9"

services:
  app:
    build:
      context: ./v0-smart-display-app
      dockerfile: Dockerfile
    ports:
      - "80:3000"
    volumes:
      - ./data:/app/data
      - ./data/photos:/app/photos
    restart: unless-stopped